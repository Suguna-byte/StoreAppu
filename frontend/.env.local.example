# Copy to .env.local for local dev. Never commit the real .env.local file.

# Server-only: where the Next.js server reaches Django (never exposed to the browser)
DJANGO_API_URL=http://127.0.0.1:8000

# Public: used for canonical URLs, sitemap, and Open Graph tags
NEXT_PUBLIC_SITE_URL=http://localhost:3000
